import numpy as np
"""we can create ndarray ,we can pass a list,tuple or any array like object into the array()method,
and it will be converted into nd array"""
arr=np.array([1,2,3,4,5])
print(arr)
print(type(arr))
print(np.__version__)